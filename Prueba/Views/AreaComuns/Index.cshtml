@model IEnumerable<Prueba.Models.AreaComun>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_LayoutAdministrador.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdInmuebleNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdInmuebleNavigation.IdInmueble)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdArea">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdArea">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdArea">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
